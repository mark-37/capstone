export interface Product {
    // name,description, manufacturer, price, quantity
    id: string;
    name: string;
    price: number;
    quantity: number;
    color: string;
    manufacturer: string;
    category: string;
    description: string;
}
